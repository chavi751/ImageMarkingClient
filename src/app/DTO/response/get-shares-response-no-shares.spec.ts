import { GetSharesResponseNoShares } from './get-shares-response-no-shares';

describe('GetSharesResponseNoShares', () => {
  it('should create an instance', () => {
    expect(new GetSharesResponseNoShares()).toBeTruthy();
  });
});
