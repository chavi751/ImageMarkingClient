import { RemoveMarkerIsNotExist } from './remove-marker-is-not-exist';

describe('RemoveMarkerIsNotExist', () => {
  it('should create an instance', () => {
    expect(new RemoveMarkerIsNotExist()).toBeTruthy();
  });
});
