import { UpdateDocumentResponseOK } from './update-document-response';

describe('UpdateDocumentResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateDocumentResponseOK()).toBeTruthy();
  });
});
