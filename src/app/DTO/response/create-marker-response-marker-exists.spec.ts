import { CreateMarkerResponseMarkerExists } from './create-marker-response-marker-exists';

describe('CreateMarkerResponseMarkerExists', () => {
  it('should create an instance', () => {
    expect(new CreateMarkerResponseMarkerExists()).toBeTruthy();
  });
});
