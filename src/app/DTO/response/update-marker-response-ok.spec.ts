import { UpdateMarkerResponseOK } from './update-marker-response-ok';

describe('UpdateMarkerResponseOK', () => {
  it('should create an instance', () => {
    expect(new UpdateMarkerResponseOK()).toBeTruthy();
  });
});
