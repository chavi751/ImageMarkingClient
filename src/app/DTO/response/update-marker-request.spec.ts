import { UpdateMarkerRequest } from './update-marker-request';

describe('UpdateMarkerRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateMarkerRequest()).toBeTruthy();
  });
});
