import { RemoveUserIsNotExist } from './remove-user-is-not-exist';

describe('RemoveUserIsNotExist', () => {
  it('should create an instance', () => {
    expect(new RemoveUserIsNotExist()).toBeTruthy();
  });
});
