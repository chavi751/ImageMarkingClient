import { SignInResponseUserUnsubscribe } from './sign-in-response-user-unsubscribe';

describe('SignInResponseUserUnsubscribe', () => {
  it('should create an instance', () => {
    expect(new SignInResponseUserUnsubscribe()).toBeTruthy();
  });
});
