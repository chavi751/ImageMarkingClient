import { SignUpResponseEmailAddressExists } from './sign-up-response-email-address-exists';

describe('SignUpResponseEmailAddressExists', () => {
  it('should create an instance', () => {
    expect(new SignUpResponseEmailAddressExists()).toBeTruthy();
  });
});
