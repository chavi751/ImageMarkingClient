import { RemoveDocumentIsNotExist } from './remove-document-is-not-exist';

describe('RemoveDocumentIsNotExist', () => {
  it('should create an instance', () => {
    expect(new RemoveDocumentIsNotExist()).toBeTruthy();
  });
});
